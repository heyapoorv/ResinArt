# uploads directory – gitkeep placeholder
# Multer uses local disk only as a fallback (not used in production since we use Cloudinary directly).
# This directory is here for reference but excluded from git via .gitignore.
